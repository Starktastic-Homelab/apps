apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cwa-book-ingest
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-pv # stays on NFS — shared RWX between calibre-web and shelfmark
  resources:
    requests:
      storage: 5Gi
