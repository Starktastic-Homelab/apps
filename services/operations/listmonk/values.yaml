controllers:
  main:
    initContainers:
      install:
        image:
          repository: listmonk/listmonk
          tag: v4.1.0@sha256:e14920d4e340ddaf5fc12ac6fe7e1a66277544427a58ad874171a17c3cfd83bd
        command: ["./listmonk", "--install", "--idempotent", "--yes"]
        envFrom:
          - secretRef:
              name: listmonk-secret
    containers:
      main:
        image:
          repository: listmonk/listmonk
          tag: v4.1.0@sha256:e14920d4e340ddaf5fc12ac6fe7e1a66277544427a58ad874171a17c3cfd83bd
        envFrom:
          - secretRef:
              name: listmonk-secret
        probes:
          liveness:
            enabled: true
            type: HTTP
            port: 9000
            path: /health
          readiness:
            enabled: true
            type: HTTP
            port: 9000
            path: /health
          startup:
            enabled: true
            type: TCP
            port: 9000
            spec:
              failureThreshold: 30
              periodSeconds: 5
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi

service:
  main:
    controller: main
    ports:
      http:
        port: 9000

persistence:
  uploads:
    existingClaim: listmonk-uploads
    globalMounts:
      - path: /listmonk/uploads
