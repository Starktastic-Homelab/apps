apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-ldap-pip-cache
  namespace: media
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: nfs-pv
  resources:
    requests:
      storage: 128Mi
