controllers:
  main:
    containers:
      main:
        image:
          repository: mccloud/subgen
          tag: 2026.03.6-cpu
          digest: sha256:10e0d0ca12b46edd3d08c1d3f81c35e5b680db774128aaf1c57b783f81ba4466
        env:
          WHISPER_MODEL: "small"
          TRANSCRIBE_DEVICE: "cpu"
          WHISPER_THREADS: "4"
          CONCURRENT_TRANSCRIPTIONS: "1"
          CLEAR_VRAM_ON_COMPLETE: "True"
          MODEL_CLEANUP_DELAY: "30"
          UPDATE: "False"
          DEBUG: "False"
        probes:
          liveness:
            enabled: true
            type: HTTP
            path: /status
            port: 9000
          readiness:
            enabled: true
            type: HTTP
            path: /status
            port: 9000
          startup:
            enabled: true
            type: HTTP
            path: /status
            port: 9000
            spec:
              initialDelaySeconds: 10
              failureThreshold: 60
              periodSeconds: 10

service:
  main:
    controller: main
    ports:
      http:
        port: 9000

persistence:
  config:
    enabled: false
  models:
    type: emptyDir
    globalMounts:
      - path: /subgen/models
