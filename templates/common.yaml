defaultPodOptions:
  securityContext:
    fsGroup: 1000

controllers:
  main:
    containers:
      main:
        env:
          TZ: "Asia/Jerusalem"
          PUID: "1000"
          PGID: "1000"
        probes:
          liveness:
            spec:
              timeoutSeconds: 5
              failureThreshold: 5
          readiness:
            spec:
              timeoutSeconds: 5
              failureThreshold: 5
          startup:
            spec:
              failureThreshold: 30
              periodSeconds: 5

persistence:
  config:
    enabled: true
    type: persistentVolumeClaim
    storageClass: "nfs-pv" # IMPORTANT: must match global.storageClass in templates/globals.yaml
    size: 1Gi
    accessMode: ReadWriteMany
    globalMounts:
      - path: /config
  media:
    enabled: false
    existingClaim: media-library-pvc
    globalMounts:
      - path: /data
