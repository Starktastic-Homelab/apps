controllers:
  main:
    containers:
      main:
        image:
          repository: eclipse-mosquitto
          tag: 2.0.22@sha256:199ea8ef2e35ec2b1b37e59cfd1dbae538ed4dfa4a2251a121a52215a6248a21
        probes:
          liveness:
            enabled: true
            type: TCP
            port: 1883
          readiness:
            enabled: true
            type: TCP
            port: 1883
          startup:
            enabled: true
            type: TCP
            port: 1883
            spec:
              failureThreshold: 30
              periodSeconds: 5

service:
  main:
    controller: main
    ports:
      mqtt:
        port: 1883

persistence:
  config:
    enabled: false
  mosquitto-data:
    existingClaim: mosquitto-config
    globalMounts:
      - path: /mosquitto/data
  mosquitto-conf:
    type: configMap
    name: mosquitto-config
    globalMounts:
      - path: /mosquitto/config/mosquitto.conf
        subPath: mosquitto.conf
  media:
    enabled: false
