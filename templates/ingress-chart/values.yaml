# Default values - overridden by app.yaml
# Global config (domains, TLS) is injected via globals.yaml
name: ""
namespace: ""

ingress:
  enabled: false
  host: ""
  domainType: "internal"  # public | internal | media
  port: 80
  serviceName: ""
  auth: false
  rateLimit: false
