apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vikunja-files
  namespace: operations
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: "nfs-pv"
  resources:
    requests:
      storage: 5Gi
