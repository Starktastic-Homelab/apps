controllers:
  main:
    containers:
      main:
        image:
          repository: listmonk/listmonk
          tag: v6.0.0@sha256:bf3903d54a468ba0544629b474a9c78714b1419ba9f89e186590264fa40d4ea1
        command: [sh, -c, "./listmonk --install --idempotent --yes --config '' && ./listmonk --upgrade --yes --config '' && ./listmonk --config ''"]
        envFrom:
          - secretRef:
              name: listmonk-secret
        probes:
          liveness:
            enabled: true
            type: HTTP
            port: 9000
            path: /health
          readiness:
            enabled: true
            type: HTTP
            port: 9000
            path: /health
          startup:
            enabled: true
            type: TCP
            port: 9000
            spec:
              failureThreshold: 30
              periodSeconds: 5
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi

service:
  main:
    controller: main
    ports:
      http:
        port: 9000

persistence:
  uploads:
    existingClaim: listmonk-uploads
    globalMounts:
      - path: /listmonk/uploads
