controllers:
  main:
    pod:
      nodeSelector:
        node-role.kubernetes.io/worker: "true"
    containers:
      main:
        image:
          repository: mccloud/subgen
          tag: 2026.06.4-cpu@sha256:6f38c07394d32883f20acc0e4d1af3df9bab43608487694332afebd7b7b6f60f
        env:
          WHISPER_MODEL: "small"
          TRANSCRIBE_DEVICE: "cpu"
          TRANSCRIBE_OR_TRANSLATE: "translate"
          WHISPER_THREADS: "6"
          CONCURRENT_TRANSCRIPTIONS: "1"
          CLEAR_VRAM_ON_COMPLETE: "True"
          MODEL_CLEANUP_DELAY: "30"
          UPDATE: "False"
          DEBUG: "False"
        probes:
          liveness:
            enabled: true
            type: HTTP
            path: /status
            port: 9000
          readiness:
            enabled: true
            type: HTTP
            path: /status
            port: 9000
          startup:
            enabled: true
            type: HTTP
            path: /status
            port: 9000
            spec:
              initialDelaySeconds: 10
              failureThreshold: 60
              periodSeconds: 10

service:
  main:
    controller: main
    ports:
      http:
        port: 9000

persistence:
  config:
    enabled: false
  media:
    enabled: true
  models:
    type: emptyDir
    globalMounts:
      - path: /subgen/models
