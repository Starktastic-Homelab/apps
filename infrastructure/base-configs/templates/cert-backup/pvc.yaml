apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cert-backup
  namespace: cert-backup
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-pv # stays on NFS — backup CronJob needs RWX
  resources:
    requests:
      storage: 1Mi
