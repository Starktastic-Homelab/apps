# Override values for qbittorrent-ru (base: services/media/qbittorrent)
# Only the delta from the base service's values.yaml
service:
  bittorrent:
    annotations:
      kube-vip.io/loadbalancerIPs: "{{ .Values.global.network.loadBalancers.qbittorrentRu }}"
    ports:
      bittorrent-tcp:
        port: 6882
      bittorrent-udp:
        port: 6882
