apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bytestash-data
  namespace: operations
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: "nfs-pv"
  resources:
    requests:
      storage: 1Gi
