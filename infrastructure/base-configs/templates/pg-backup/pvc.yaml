apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-backup
  namespace: databases
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-pv # stays on NFS — backup CronJob needs RWX
  resources:
    requests:
      storage: 1Gi
